# Homebrew tap formula template
#
# This file is copied into Fetch-Hive/homebrew-tap (not yet created) after the
# first public release. Replace placeholder SHA-256 values with SHA256SUMS from
# the GitHub Release.
